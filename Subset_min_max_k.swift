// The number of subsets S with min(S) + max(S) <= K
/**
http://www.1point3acres.com/bbs/thread-236140-1-1.html
给一个set，求有多少个subsets，里面的min和max之和小于k。
国人小哥很nice的提示了可以先排序，然后two pointer从两头往中间找。

http://www.1point3acres.com/bbs/forum.php?mod=viewthread&tid=110783
find the maximum number in an integer array. The numbers in the array increase first, then decreases. Maybe there’s only increase or decrease. 先说了直接扫一遍，是O(n), 然后用binary search 就是O(log n).
*/
